# Model Card

**Project**: Housing Price Prediction
**Author**: root
**Date**: 2025-09-15 21:42:32
**Environment**: Python 3.12.11

## Data
- Source: [your dataset name or link]
- Train size: 2745 rows
- Valid size: 687 rows
- Test size: 859 rows
- Target transform: log

## Model
- Final winner: CatBoost
- Base model: CatBoost

## Performance
On **test set**:
- RMSE: 0.17458
- MAE: 0.11985
- R²: 0.87016
- Median absolute % error: 0.07879

## Interpretation
- Top features: square_meters, total_area, district, ownership, layout
- See `plots/` for permutation importance, PDP, SHAP plots.

## Limitations
- May not generalize to regions/markets not present in training data.
- Sensitive to feature encoding and target transformation.

## Usage
Load artifacts with `joblib.load()` from `deployable/`.
Use `predict_price(df)` function to get predictions.
- Input: raw dataframe with same schema as training data.
- Output: predicted housing price (float).
